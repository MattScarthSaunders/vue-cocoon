{
  "name": "vue-cocoon",
  "version": "0.1.5",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "docs": "typedoc",
    "preversion": "npm run build",
    "postversion": "git push origin main && git push origin --tags && npm publish"
  },
  "peerDependencies": {
    "vue": "^3.2.0"
  },
  "devDependencies": {
    "typedoc": "^0.28.15",
    "typescript": "^5.0.0",
    "vue": "^3.5.26"
  },
  "files": [
    "dist",
    "README.md"
  ]
}
